# Scenario Catalog

This catalog defines the first parity scenarios for the whitebox effort. Each scenario should eventually have a fixture, oracle capture, and machine-checkable pass criteria.

## Machine-Readable Evidence

For scenarios that are already wired into the oracle workflow:

- capture evidence lives in `oracle_capture_output/<bundle>/manifest.json` under `scenario_index`
- whitebox parity evidence lives in `oracle_capture_output/<bundle>/whitebox_compare_report.json` under `scenario_results`

The current macOS arm bundle covers `SIM-PT-001`, `SIM-PT-002`, `SIM-PT-003`, `SIM-PT-004`, `SIM-PT-005`, `SIM-PT-006`, `SIM-PT-007`, `SIM-PT-008`, `SIM-PT-009`, `SIM-PT-010`, `SIM-PT-011`, `SIM-PT-012`, `SIM-PT-013`, `SIM-PT-014`, `SIM-PT-015`, `SIM-PT-016`, `SIM-PT-017`, `SIM-PT-018`, `SIM-PT-019`, `SIM-PT-020`, `SIM-PT-021`, `SIM-PT-022`, `SIM-PT-023`, `SIM-PT-024`, `SIM-PT-025`, `SIM-PT-026`, `SIM-PT-027`, `SIM-PT-028`, `SIM-PT-029`, `SIM-PT-030`, `SIM-PT-031`, `SIM-PT-032`, `SIM-MESH-001`, `SIM-MESH-002`, `LIDAR-001`, `LIDAR-002`, `LIDAR-003`, `RCS-001`, `RCS-002`, and `RCS-003`.

## Scenario Table

| ID | Focus | Inputs | Expected Artifacts | Comparator | Pass Rule | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `PROC-001` | `range_fft` basic correctness | synthetic complex baseband cube with one tone and no window | range FFT cube | exact shape, near-exact FFT bins | shape exact, relative error within floating-point noise | `processing.py:34-58` |
| `PROC-002` | `range_doppler_fft` chained behavior | deterministic cube with known range and Doppler bins, custom windows, zero padding | range-Doppler cube | axis placement and bin location | shape exact, peak bin indices match, amplitude within tight tolerance | `processing.py:88-117` |
| `PROC-003` | CA-CFAR thresholds | real-valued 1-D and 2-D magnitude maps with known guard/trailing settings | threshold arrays | compare threshold arrays cell-by-cell | same shape, same invalid-input behavior, tight numerical tolerance | `processing.py:120-260` |
| `PROC-004` | OS-CFAR rank behavior | real-valued arrays with deterministic sorted neighborhoods | threshold arrays | compare threshold arrays and rank edge cases | same shape, same error handling, tight numerical tolerance | `processing.py:263-536` |
| `PROC-005` | DoA estimators | synthetic covariance matrices for one-source and two-source ULAs | estimated angles and spectra | angle ordering and spectrum peak positions | peak angles within agreed tolerance, output container shape stable | `processing.py:539-823` |
| `TOOL-001` | ROC and Swerling utilities | fixed `pfa`, `snr`, `npulses`, and target model cases | scalar or vector Pd/SNR outputs | compare against blackbox or static reference formulas | tight numerical tolerance across representative inputs | `tools.py:47-566` |
| `MODEL-001` | `Transmitter` scalar waveform defaults | scalar `f`, scalar `t`, default channel list | `rf_prop`, `waveform_prop`, `txchannel_prop` | dict-key, default-expansion, and dtype comparison | exact keys, exact pulse defaults, shape exact | `transmitter.py:214-288` |
| `MODEL-002` | `Transmitter` modulation and channel patterns | multi-channel input with pulse modulation, waveform modulation, custom patterns | channel dictionaries and modulation arrays | compare normalized patterns, gains, and complex modulation arrays | exact keys, exact shapes, tight numerical parity | `transmitter.py:365-530` |
| `MODEL-003` | `Receiver` `bb_type` behavior | same receiver inputs with `complex` and `real` baseband modes | `bb_prop` and `rxchannel_prop` | compare noise bandwidth and channel normalization | exact key set, exact bandwidth rules, shape exact | `receiver.py:150-299` |
| `MODEL-004` | `Radar` single-frame timestamp and virtual array | small TX/RX array with scalar motion and one frame | `time_prop`, `sample_prop`, `array_prop` | compare channel ordering, timestamp cube, and virtual array geometry | exact shapes and exact timestamp values | `radar.py:418-608` |
| `MODEL-005` | `Radar` multi-frame timestamp flattening | multiple frame start times and static motion | flattened `timestamp` array | compare frame-major flattening order and offsets | exact shape and exact per-frame offsets | `radar.py:620-643` |
| `MODEL-006` | `Radar.set_motion` time-varying rules | timestamp-shaped location or rotation arrays with legal and illegal speed inputs | updated `radar_prop` or raised errors | compare acceptance and rejection behavior | exact error conditions and exact stored-array shapes | `radar.py:676-881` |
| `SIM-PT-001` | `sim_radar` minimal point target | one TX, one RX, one static point target | result dict and `baseband` | compare key existence, shape, and phase progression | exact keys once verified, shape exact, tight amplitude/phase tolerance | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-001`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-001` |
| `SIM-PT-002` | `sim_radar` MIMO ordering | multiple TX and RX channels with one target | result dict and `baseband` cube | compare channel ordering and virtual-array mapping | exact axis ordering and channel-to-channel phase relationships | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-002`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-002` |
| `SIM-PT-003` | `sim_radar` seed and noise behavior | fixed radar seed with phase-noise inputs and repeated runs | result dict plus noise-sensitive fields | compare run-to-run reproducibility | same seeded outputs across repeated runs on same oracle baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-003`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-003` |
| `SIM-PT-004` | `sim_radar` two-point-target separation | one TX, one RX, two static point targets at distinct ranges | result dict and range profile | compare range-profile dominant bins and normalized profile similarity | timestamp and shape must match, dominant range bins must match exactly, and range-profile correlation must clear the accepted threshold | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-004`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-004` |
| `SIM-PT-005` | `sim_radar` MIMO multi-frame flattening | two TX, two RX, two frames, one moving point target | result dict and `baseband` cube | compare exact timestamp flattening, frame-block offsets, and waveform parity | shape must be `[8, 4, 160]`, frame blocks must be separated by the exact frame-time offset, and waveform parity must remain exact within the accepted tolerance | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-005`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-005` |
| `SIM-PT-006` | `sim_radar` point-target phase rotation | one TX, one RX, one static point target with `phase=90.0` | result dict and `baseband` cube | compare exact waveform parity and quadrature ratio relative to the baseline point-target artifact | shape must remain `[1, 4, 160]`, timestamps must match exactly, and the phase-tagged waveform must remain a quadrature rotation of the baseline waveform on both vendor and whitebox runs | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-006`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-006` |
| `SIM-PT-007` | `sim_radar` single-frame moving-point Doppler | one TX, one RX, one point target with `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus dominant range-Doppler peak | shape must remain `[1, 4, 160]`, timestamps must match exactly, waveform parity must remain exact within the accepted tolerance, and the dominant range-Doppler peak must match the captured moving-target baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-007`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-007` |
| `SIM-PT-008` | `sim_radar` off-axis MIMO spatial phase | two TX, two RX, one static point target at `location=[50.0, 1.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus channel-relative phase at the captured peak sample | shape must remain `[4, 4, 160]`, timestamps must match exactly, waveform parity must remain exact within the accepted tolerance, and the channel-relative spatial phase pattern must match the captured off-axis MIMO baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-008`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-008` |
| `SIM-PT-009` | `sim_radar` off-axis moving MIMO joint contract | two TX, two RX, one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus dominant range-Doppler peak and channel-relative phase at captured sample index `2` | shape must remain `[4, 4, 160]`, timestamps must match exactly, waveform parity must remain exact within the accepted tolerance, the dominant range-Doppler peak must match, and the captured off-axis moving MIMO phase pattern at sample index `2` must match | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-009`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-009` |
| `SIM-PT-010` | `sim_radar` off-axis moving MIMO multi-frame joint contract | two TX, two RX, two frames, one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus frame-block timestamp flattening, dominant range-Doppler peak, and first-frame channel-relative phase at captured sample index `2` | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, waveform parity must remain exact within the accepted tolerance, the dominant range-Doppler peak must match, and the captured first-frame off-axis moving MIMO phase pattern at sample index `2` must match | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-010`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-010` |
| `SIM-PT-011` | `sim_radar` negative-radial-speed Doppler direction | one TX, one RX, one point target with `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus dominant range-Doppler peak | shape must remain `[1, 4, 160]`, timestamps must match exactly, waveform parity must remain exact within the accepted tolerance, and the dominant range-Doppler peak must match the captured negative-speed baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-011`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-011` |
| `SIM-PT-012` | `sim_radar` off-axis negative-speed MIMO joint contract | two TX, two RX, one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus dominant range-Doppler peak and channel-relative phase at captured sample index `2` | shape must remain `[4, 4, 160]`, timestamps must match exactly, waveform parity must remain exact within the accepted tolerance, the dominant range-Doppler peak must match, and the captured off-axis negative-speed MIMO phase pattern at sample index `2` must match | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-012`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-012` |
| `SIM-PT-013` | `sim_radar` off-axis negative-speed MIMO multi-frame joint contract | two TX, two RX, two frames, one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact waveform parity plus frame-block timestamp flattening, dominant range-Doppler peak, and first-frame channel-relative phase at captured sample index `2` | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, waveform parity must remain exact within the accepted tolerance, the dominant range-Doppler peak must match, and the captured first-frame off-axis negative-speed MIMO phase pattern at sample index `2` must match | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-013`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-013` |
| `SIM-PT-014` | `sim_radar` static-plus-moving two-target separation | one static point target at `20 m` and one moving point target at `50 m` with `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare local static and moving range-Doppler peaks plus profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the static local range-Doppler peak must remain `(0, 27)`, the moving local range-Doppler peak must remain `(1, 67)`, normalized range-profile correlation must clear the accepted threshold, and waveform correlation must remain high enough for the mixed-target baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-014`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-014` |
| `SIM-PT-015` | `sim_radar` static-plus-negative-moving two-target separation | one static point target at `20 m` and one moving point target at `50 m` with `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare local static and moving range-Doppler peaks plus profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the static local range-Doppler peak must remain `(0, 27)`, the negative-speed moving local range-Doppler peak must remain `(3, 67)`, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` for the mixed-target baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-015`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-015` |
| `SIM-PT-016` | `sim_radar` off-axis MIMO static-plus-moving joint contract | 2-TX x 2-RX radar with one static point target at `20 m` and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel local static and moving range-Doppler peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's static local range-Doppler peak must remain `(0, 27)`, each channel's moving local range-Doppler peak must remain `(1, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-016`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-016` |
| `SIM-PT-017` | `sim_radar` off-axis MIMO static-plus-negative-moving joint contract | 2-TX x 2-RX radar with one static point target at `20 m` and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel local static and moving range-Doppler peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's static local range-Doppler peak must remain `(0, 27)`, each channel's negative-speed moving local range-Doppler peak must remain `(3, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-017`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-017` |
| `SIM-PT-018` | `sim_radar` off-axis MIMO static-plus-moving multi-frame joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m` and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel local static and moving range-Doppler peaks, first-frame channel-relative phase, frame-block offset, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's static local range-Doppler peak must remain `(0, 27)`, each channel's moving local range-Doppler peak must remain `(1, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-018`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-018` |
| `SIM-PT-019` | `sim_radar` off-axis MIMO static-plus-negative-moving multi-frame joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m` and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel local static and moving range-Doppler peaks, first-frame channel-relative phase, frame-block offset, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's static local range-Doppler peak must remain `(0, 27)`, each channel's negative-speed moving local range-Doppler peak must remain `(3, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-019`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-019` |
| `SIM-PT-020` | `sim_radar` static-plus-static-plus-moving three-target separation | one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `50 m` with `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare two static local range-Doppler peaks, one moving local range-Doppler peak, and profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the first static local range-Doppler peak must remain `(0, 27)`, the second static local range-Doppler peak must remain `(0, 47)`, the moving local range-Doppler peak must remain `(1, 67)`, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-020`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-020` |
| `SIM-PT-021` | `sim_radar` static-plus-static-plus-negative-moving three-target separation | one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `50 m` with `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare two static local range-Doppler peaks, one moving local range-Doppler peak, and profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the first static local range-Doppler peak must remain `(0, 27)`, the second static local range-Doppler peak must remain `(0, 47)`, the negative-speed moving local range-Doppler peak must remain `(3, 67)`, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-021`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-021` |
| `SIM-PT-022` | `sim_radar` off-axis MIMO static-plus-static-plus-moving joint contract | 2-TX x 2-RX radar with one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static and moving range-Doppler peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's moving local range-Doppler peak must remain `(1, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-022`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-022` |
| `SIM-PT-023` | `sim_radar` off-axis MIMO static-plus-static-plus-negative-moving joint contract | 2-TX x 2-RX radar with one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static and moving range-Doppler peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's negative-speed moving local range-Doppler peak must remain `(3, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-023`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-023` |
| `SIM-PT-024` | `sim_radar` off-axis MIMO static-plus-static-plus-moving multi-frame joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static and moving range-Doppler peaks, first-frame channel-relative phase, frame-block offset, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's moving local range-Doppler peak must remain `(1, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-024`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-024` |
| `SIM-PT-025` | `sim_radar` off-axis MIMO static-plus-static-plus-negative-moving multi-frame joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m`, one static point target at `35 m`, and one moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static and moving range-Doppler peaks, first-frame channel-relative phase, frame-block offset, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's negative-speed moving local range-Doppler peak must remain `(3, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-025`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-025` |
| `SIM-PT-026` | `sim_radar` static-plus-static-plus-moving-plus-moving four-target separation | one static point target at `20 m`, one static point target at `35 m`, one moving point target at `10 m` with `speed=[8.0, 0.0, 0.0]`, and one moving point target at `50 m` with `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare two static local range-Doppler peaks, two moving local range-Doppler peaks, and profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the first static local range-Doppler peak must remain `(0, 27)`, the second static local range-Doppler peak must remain `(0, 47)`, the first moving local range-Doppler peak must remain `(1, 14)`, the second moving local range-Doppler peak must remain `(1, 67)`, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-026`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-026` |
| `SIM-PT-027` | `sim_radar` static-plus-static-plus-negative-moving-plus-negative-moving four-target separation | one static point target at `20 m`, one static point target at `35 m`, one moving point target at `10 m` with `speed=[-8.0, 0.0, 0.0]`, and one moving point target at `50 m` with `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare two static local range-Doppler peaks, two negative-speed moving local range-Doppler peaks, and profile correlation | shape must remain `[1, 4, 160]`, timestamps must match exactly, the first static local range-Doppler peak must remain `(0, 27)`, the second static local range-Doppler peak must remain `(0, 47)`, the first negative-speed moving local range-Doppler peak must remain `(3, 13)`, the second negative-speed moving local range-Doppler peak must remain `(3, 66)`, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-027`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-027` |
| `SIM-PT-028` | `sim_radar` off-axis MIMO static-plus-static-plus-moving-plus-moving four-target joint contract | 2-TX x 2-RX radar with one static point target at `20 m`, one static point target at `35 m`, one off-axis moving point target at `location=[10.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]`, and one off-axis moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static peaks, per-channel dual-moving peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's first moving local range-Doppler peak must remain `(1, 14)`, each channel's second moving local range-Doppler peak must remain `(1, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-028`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-028` |
| `SIM-PT-029` | `sim_radar` off-axis MIMO static-plus-static-plus-moving-plus-moving multi-frame four-target joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m`, one static point target at `35 m`, one off-axis moving point target at `location=[10.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]`, and one off-axis moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact frame-block flattening, per-channel dual-static peaks, per-channel dual-moving peaks, first-frame channel-relative phase, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's first moving local range-Doppler peak must remain `(1, 14)`, each channel's second moving local range-Doppler peak must remain `(1, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-029`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-029` |
| `SIM-PT-030` | `sim_radar` off-axis MIMO static-plus-static-plus-mixed-sign-moving four-target joint contract | 2-TX x 2-RX radar with one static point target at `20 m`, one static point target at `35 m`, one off-axis moving point target at `location=[10.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]`, and one off-axis moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare per-channel dual-static peaks, positive- and negative-moving peaks, channel-relative phase, and correlation | shape must remain `[4, 4, 160]`, timestamps must match exactly, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's positive-moving local range-Doppler peak must remain `(1, 14)`, each channel's negative-moving local range-Doppler peak must remain `(3, 67)`, channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-030`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-030` |
| `SIM-PT-031` | `sim_radar` off-axis MIMO static-plus-static-plus-mixed-sign-moving multi-frame four-target joint contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m`, one static point target at `35 m`, one off-axis moving point target at `location=[10.0, 1.0, 0.0]`, `speed=[8.0, 0.0, 0.0]`, and one off-axis moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact frame-block flattening, per-channel dual-static peaks, positive- and negative-moving peaks, first-frame channel-relative phase, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's positive-moving local range-Doppler peak must remain `(1, 14)`, each channel's negative-moving local range-Doppler peak must remain `(3, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-031`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-031` |
| `SIM-PT-032` | `sim_radar` off-axis MIMO static-plus-static-plus-negative-moving-plus-negative-moving multi-frame four-target accepted-band contract | 2-TX x 2-RX x 2-frame radar with one static point target at `20 m`, one static point target at `35 m`, one off-axis moving point target at `location=[10.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]`, and one off-axis moving point target at `location=[50.0, 1.0, 0.0]`, `speed=[-8.0, 0.0, 0.0]` | result dict and `baseband` cube | compare exact frame-block flattening, per-channel dual-static peaks, near negative-moving peak, accepted far negative-moving local band, first-frame channel-relative phase, and correlation | shape must remain `[8, 4, 160]`, timestamps must match exactly, the second frame block must be offset by exactly `+0.001` seconds, each channel's first static local range-Doppler peak must remain `(0, 27)`, each channel's second static local range-Doppler peak must remain `(0, 47)`, each channel's near negative-moving local range-Doppler peak must remain `(3, 13)`, each channel's far negative-moving local range-Doppler peak must remain within the accepted band `(3, 66)` or `(3, 67)`, first-frame channel-relative phase at sample index `2` must match exactly, normalized range-profile correlation must be at least `0.999`, and waveform correlation must be at least `0.995` | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-PT-032`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-PT-032` |
| `SIM-MESH-001` | mesh ingest plus radar scene handoff | simple triangle mesh loaded through `mesh_kit`, then used in a radar scene | mesh dict and simulator result | compare mesh points/cells and simulator acceptance | mesh contract exact, simulator behavior to oracle tolerance | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-MESH-001`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-MESH-001` |
| `SIM-MESH-002` | mesh aspect sensitivity | same planar mesh at broadside and yawed edge-on poses | result dict and `baseband` cube | compare aggregate return strength trend | broadside return norm exceeds edge-on return norm on the accepted oracle baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.SIM-MESH-002`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.SIM-MESH-002` |
| `RCS-001` | single-object RCS baseline | one mesh object, one frequency, fixed aspect angles | RCS output object | compare output container and numeric response | container exact, numeric tolerance chosen after oracle study | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.RCS-001`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.RCS-001` |
| `RCS-002` | angle sweep RCS stability | same object over observation-angle sweep with broadside illumination | RCS sweep array | compare main-lobe location, early monotonic decay, quiet tail, and normalized curve correlation | peak index must match, first three sweep samples must decrease, post-90-degree tail must stay quiet, and normalized correlation must pass the accepted threshold | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.RCS-002`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.RCS-002` |
| `RCS-003` | polarization discrimination | same broadside plate with fixed incident polarization and multiple observation polarizations | RCS case array | compare co-polar magnitude and cross-polar quietness | case names must match, co-pol response must stay within accepted tolerance, and all cross-pol cases must remain effectively quiet relative to co-pol | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.RCS-003`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.RCS-003` |
| `LIDAR-001` | LiDAR point cloud baseline | simple scene with a known planar object | point cloud result | compare hit count, geometry bounds, and ordering rules | exact or geometric tolerance after oracle capture | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.LIDAR-001`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.LIDAR-001` |
| `LIDAR-002` | LiDAR frame-time motion | moving mesh target with fixed `speed` and repeated scans | point cloud result at two timestamps | compare hit displacement against target motion | hit geometry shifts by expected rigid translation on the accepted oracle baseline | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.LIDAR-002`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.LIDAR-002` |
| `LIDAR-003` | LiDAR multi-hit ordering and reflection directions | origin-centered lidar with three horizontal rays and three separated planar targets | point cloud result with three hits | compare hit ordering, hit positions, reflected directions, and derived distances | hit count must match, y-order must be preserved, shared vendor fields must match, and derived distances must agree with vendor hit geometry | `oracle_capture_output/macos_arm_py311/manifest.json` -> `scenario_index.LIDAR-003`, `oracle_capture_output/macos_arm_py311/whitebox_compare_report.json` -> `scenario_results.LIDAR-003` |

## Minimum Coverage Rule

Before implementation begins on any subsystem, at least one matching scenario from this catalog must be chosen as the acceptance target.

## Comparator Policy

- Use exact dictionary keys and array shapes whenever the Python layer makes them explicit.
- Use tight numeric tolerances for `processing.py`, `tools.py`, and model-layer contracts.
- Use statistical or geometric tolerances for mesh, RCS, and LiDAR work only after oracle behavior is observable.
